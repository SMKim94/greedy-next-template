export const dynamic = 'force-dynamic';

const Home = async () => {
  return (
    <main>
      <h1 className="text-2xl font-thin underline">Hello, World!</h1>
    </main>
  );
};

export default Home;
